[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = ["dotnet restore", "dotnet publish ContentHub.Api/ContentHub.Api.csproj -c Release -o out"]

[start]
cmd = "dotnet out/ContentHub.Api.dll"